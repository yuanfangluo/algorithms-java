package Algorithms._0_核心框架汇总.双指针技巧秒杀七道数组题目.快慢指针.滑动窗口算法;

public class 滑动窗口算法框架 {
    // 滑动窗口算法框架伪码
    // int left = 0, right = 0;

    // while (right < nums.size()) {
    // // 增大窗口
    // window.addLast(nums[right]);
    // right++;

    // while (window needs shrink) {
    // // 缩小窗口
    // window.removeFirst(nums[left]);
    // left++;
    // }
    // }
}
